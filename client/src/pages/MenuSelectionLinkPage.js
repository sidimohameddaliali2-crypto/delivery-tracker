import React from 'react';
import MenuSelectionLink from '../components/menuSelectionLink/MenuSelectionLink';

// Standalone design preview for "Menu Selection Link.dc.html" (demo data only).
// The real redesigned flow is served from /menu-select/:token and only for the
// configured test menu — see components/menuSelectionLink/config.js.
const MenuSelectionLinkPage = () => <MenuSelectionLink />;

export default MenuSelectionLinkPage;
